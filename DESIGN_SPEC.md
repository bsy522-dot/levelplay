# 배움퀘스트 디자인 스펙 (개편 v15)

작성 2026-08-05 · 근거: `D:\AI\05_분석\앱출시포트폴리오\교육앱_첫화면_벤치마크.md`
(국내 티니핑 6종 + 해외 6종 스크린샷 실측 — 다크 배경 0/12, 이모지 썸네일 0/12, 마스코트=아이콘 11/12)

---

## 0. 3원칙

1. **밝게** — 아동 교육앱에서 다크 기본값 사례는 없다. 5~6.5세는 검정/회색을 밤·죽음과 연결한다(Boyatzis & Varghese 1994).
2. **주제가 먼저, 형식은 필터** — 학습/놀이 칸막이를 없애고 과목으로 묶는다.
3. **이모지 금지** — 콘텐츠 썸네일·과목 아이콘에 유니코드 이모지를 쓰지 않는다. 전용 SVG 또는 실제 화면 이미지.

---

## 1. 컬러 토큰 (라이트 = 기본값)

```css
:root{
  --bg:#F4F7FF;   /* 페이지 배경 — 아주 옅은 블루화이트 */
  --c1:#FFFFFF;   /* 카드 표면 */
  --c2:#E9EFFB;   /* 보조 표면 · 칩 · 입력 */
  --tx:#1A2140;   /* 본문 (대비 14.9:1) */
  --t2:#525C7A;   /* 보조 텍스트 (7.0:1) */
  --t3:#6E7897;   /* 3차 텍스트 (5.0:1) */
  --p :#6B45F0;   /* 보라 액센트 — 흰 배경 대비 5.6:1 */
  --cy:#00875F;   /* 민트 액센트(텍스트용) — 4.6:1  ※원래 #06d6a0은 1.7:1로 부적격 */
  --gd:#B26A00;   /* 골드 액센트(텍스트용) — 4.5:1  ※원래 #fbbf24는 1.9:1로 부적격 */
  --rd:#D92D45;
  --gn:#12874A;
  --g1:linear-gradient(135deg,#7C5CFF,#00C9A7); /* 버튼 배경 — 그 위 글씨는 #fff 유지 */
  --r:16px;       /* 라운드 강화 (기존 12px) — 벤치마크 공통 조형 */
  --nv:58px;      /* 하단 내비 높이 (기존 52px) — 아동 터치 타깃 */
}
```

### 대비 규칙 (반드시 지킬 것)
- `--cy` / `--gd` / `--p` 는 **텍스트 색으로 141 / 33 / 15회** 쓰인다. 흰 배경에서 AA(4.5:1)를 넘겨야 하므로 위 값으로 어둡게 잡았다.
- 반대로 **그라디언트 버튼(`--g1`) 위 글씨는 계속 `#fff`** 로 둔다. 이건 어두운 배경이므로 문제없다.
- 저알파 틴트(`rgba(139,92,246,.08)`, `rgba(6,214,160,.10)` 등 134곳)는 흰 배경에서도 은은한 색면으로 작동하므로 **건드리지 않는다.**

### 다크 모드
`:root.dark` 로 **옵트인**만 제공한다(중고등·성인 프로필 및 수동 토글). 기본값은 라이트.
기존 `:root.light` 규칙 7개는 역할이 뒤집히므로 정리한다.
게임 내부 화면(`games/*.html`)은 **다크 유지** — 몰입에 유리하고 38개 게임 회귀 리스크를 피한다.

---

## 2. 과목 색 (8과목)

각 과목은 고유 액센트 1색을 가진다. 카드 좌측 4px 바 · 아이콘 배경 틴트에 쓴다.

| 과목 | 액센트 | 틴트(배경) |
|---|---|---|
| 한국사 | `#C2410C` | `#FFF1E9` |
| 수학 | `#1D4ED8` | `#E8EFFF` |
| 과학 | `#0E7490` | `#E2F5F8` |
| 음악 | `#7C3AED` | `#F1EAFE` |
| 체육 | `#15803D` | `#E7F7EC` |
| 언어(국어·영어) | `#B91C1C` | `#FEECEC` |
| 예술 | `#BE185D` | `#FDEBF3` |
| 경제·교양 | `#A16207` | `#FCF3E0` |

---

## 3. 정보 구조 (IA)

### 하단 탭 (5개)
```
홈 · 과목 · 지도 · 커뮤 · 나
```
- **학습/놀이 칸막이 제거.** 형식(영상·게임·만화)은 과목 안의 **필터**로 내린다.
- 아이콘은 이모지 금지 — 전용 라인 SVG.

### 홈 (첫 화면)
벤치마크 공통: **다음에 뭘 눌러야 하는지가 화면에 딱 하나.**
```
1. 마스코트 인사 + 오늘의 한 가지  ← 유일한 주 액션 (큰 버튼)
2. 이어서 하기 (진행 중인 것)
3. 내 과목 타일 (8과목, 진도 표시)
4. 새로 나온 것
```
검색바는 상단 아이콘으로 축소한다(주 액션과 경쟁 금지).

### 과목 허브 (신설)
과목 하나를 누르면:
```
[배우기]  영상 · 만화 · 레슨
[해보기]  게임 · 실험
[확인]    퀴즈 · 미니시험
[이어서]  다른 과목으로 건너가는 연결
```

### 연결(그래프)
콘텐츠마다 `subject` / `topic` / `era` 태그를 부여하고, 태그가 겹치는 항목을 "이어서"에 자동 노출.
- 예: 한국사 영웅전(게임) ↔ 고조선 만화 ↔ 아침의 나라 임금님들 ↔ 역사 퀴즈
- 예: 색소폰/노래방(게임) ↔ 모차르트 영상 ↔ 악보 레슨
- 예: 복싱/골프(게임) ↔ 체육 영상 ↔ 몸의 원리(과학)
"지도" 탭은 이 태그 그래프를 점과 선으로 시각화한다.

---

## 4. 썸네일 (이모지 전량 교체)

| 대상 | 교체 이미지 | 소스 |
|---|---|---|
| 게임 카드 | 실제 플레이 화면 | `assets/thumbs/*.jpg` (Playwright 자동 촬영) |
| 만화 레슨 | 만화 첫 컷 | `assets/comics/**/p1.jpg` |
| 영상 | 유튜브 썸네일 | 현행 유지(정상) |
| 과목 타일 | 과목별 전용 SVG | 신규 제작 |
| 하단 탭 | 라인 SVG | 신규 제작 |

`ic:'🔢'` 형태의 이모지 아이콘 필드 100곳이 대상.

---

## 5. 마스코트 "한범" (호랑이)

역할 모델은 **Khan Academy Kids의 Kodi형 — 온화한 학습 동반자**.
듀오링고식 죄책감 알림("네가 Duo를 슬프게 했어")은 채택하지 않는다.

### 조형 원칙 (듀오링고 공개 원칙 채택)
> geometric simplicity · large expressive eyes · unique body shape · detached feet

- 원·삼각형 등 기본 도형 조합으로만 그린다. (Duo = "얼굴 달린 치킨너겟", "날개 달린 원통")
- **48px 앱 아이콘에서 귀 실루엣만으로 판별**되는지 매번 검사한다.
- 표정은 **기쁨 / 생각중** 2종부터. (2018년 이전 Duo도 2종뿐이었다)
- 정답 시 응원, 오답 시 앞발을 턱에 대는 "같이 생각하는" 제스처 — Kodi의 교수법적 스캐폴딩을 따른다.

### 연령별 변주 (캐릭터를 새로 만들지 않고 톤만)
| 연령 | 마스코트 | 화면 톤 |
|---|---|---|
| 유아 5~7 | 동글·파스텔·볼터치 | 최대 밝기, 글자 크게, 아이콘 위주 |
| 초등 8~12 | 줄무늬 등장·채도↑ | 기본 라이트 |
| 중고등 | 눈 가늘게·각지게 | 다크 허용, 정보밀도↑ |
| 성인 | 캐릭터 제거·심볼만 | 미니멀, 통계 중심 |

### 앱 아이콘
마스코트 얼굴이 그대로 앱 아이콘이 된다(벤치마크 11/12가 그렇다). "LP" 이니셜 아이콘은 폐기.

---

## 6. 접근성

- 본문 최소 4.5:1, 큰 글씨 3:1.
- 터치 타깃 최소 48×48.
- 유아 모드에서 핵심 버튼에 **음성 안내**(ABCmouse가 글 못 읽는 아이를 위해 채택).

---

## 7. 검증 기준 (각 단계 완료 조건)

1. **테마** — 홈·과목·놀이·커뮤·나 5개 탭 스크린샷에서 검은 배경이 남아있지 않을 것. 대비 위반 0건.
2. **썸네일** — 게임·레슨 카드에 이모지가 하나도 없을 것.
3. **IA** — 음악 허브 한 화면에서 영상·게임·레슨·퀴즈가 모두 잡힐 것. 한국사 게임 종료 시 고조선 만화로 이어질 것.
4. **마스코트** — 홈 첫 화면에 마스코트가 있고, 앱 아이콘과 같은 얼굴일 것.
5. **연령** — 유아/중고등 프로필 전환 시 첫 화면이 실제로 달라질 것.
6. 모든 단계는 **실제 렌더 스크린샷으로 확인**한 뒤에만 완료로 본다.
