# LevelPlay 배포·운영 안내 (쉬운 말)

이 사이트는 **GitHub Pages**로 자동 공개됩니다.
공개 주소: https://bsy522-dot.github.io/levelplay/

> 아래 작업은 보통 **비서(Claude)가 대신 처리**합니다. 직접 하실 필요는 없지만,
> "무슨 일이 일어나는지" 알 수 있게 쉬운 말로 적어둡니다.

---

## 1. 새 내용을 올리면 어떻게 공개되나요?
- 변경 내용을 저장(커밋)하고 올리면(푸시), **약 1~2분 뒤** 사이트에 자동 반영됩니다.
- 바로 안 보이면 브라우저에서 **강력 새로고침**(`Ctrl`+`F5`) 한 번 해주세요.
- 휴대폰 앱(홈화면 추가본)은 잠시 뒤 자동으로 최신본을 받습니다(서비스워커가 처리).

## 2. 문제가 생기면 되돌릴 수 있나요? (롤백)
- 네. 모든 작업은 **단계마다 저장(되돌리기 지점)** 되어 있습니다.
- 문제가 생기면 비서에게 **"직전 안전 지점으로 되돌려줘"** 라고만 말씀하시면 됩니다.
- 2026-06-10 개선 작업의 **직전 안전 지점**: `d6c815af` (개선 전 마지막 상태).
  - 개인정보 차단 직후 지점: `91d18530`
  - 전체 개선 완료 지점: 최신(`main` 끝)

## 3. 개인정보·민감 파일이 다시 올라가지 않게 막아뒀습니다
- `.gitignore`에 차단 규칙을 넣어, 아래 종류는 **앞으로 자동으로 공개되지 않습니다**:
  - 개인 대화/세션 파일, 워드(.docx)·PPT(.pptx), 백업(.bak), 앱설치(apk), 임시·테스트 파일.
- ⚠️ 참고: 이미 지난 기록(과거 저장본)에는 흔적이 남을 수 있습니다.
  완전 삭제가 필요하면 비서에게 "과거 기록까지 완전 정리" 를 요청하세요(별도 작업).

## 4. 핵심 파일 안내
| 파일 | 역할 |
|------|------|
| `index.html` | 앱 본체(홈·학습·게임·커뮤니티·프로필) |
| `v3~v7_patch.js` | 기능 추가본(앱이 켜질 때 함께 로드) |
| `sw.js` | 오프라인·캐시 담당(서비스워커). 고치면 `CACHE_NAME` 숫자를 올려야 반영됨 |
| `manifest.json` | "홈 화면에 추가"(앱 설치) 정보 |
| `games/` | 개별 게임들 |
| `data/` | 학습 콘텐츠·퀴즈 데이터 |

## 5. 점검 방법(비서용)
- 빌드 도구가 없는 정적 사이트입니다. "빌드 검사" = 아래로 대신합니다.
  - 문법 검사: `node --check <파일>` (패치 JS), 인라인 스크립트 검사 스크립트.
  - 동작 검사: 로컬 서버 띄우고 실제 브라우저(Edge)로 E2E + 스크린샷 비교.
- 배포 = `main` 브랜치에 푸시(=GitHub Pages 자동 빌드).
